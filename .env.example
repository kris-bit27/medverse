# =============================================================================
# MedVerse EDU — Environment Variables
# Copy to .env.local and fill in values
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE (required)
# Get from: https://supabase.com/dashboard → Settings → API
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Storage bucket name (default: uploads)
# VITE_SUPABASE_STORAGE_BUCKET=uploads

# -----------------------------------------------------------------------------
# AI PROVIDERS (required — at least one)
# -----------------------------------------------------------------------------

# Anthropic Claude — primary AI for content generation and copilot
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# OpenAI GPT-4o — independent cross-model content review
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini — fallback for high-yield mode in generate-topic
# Get from: https://aistudio.google.com/apikey
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-pro
# GEMINI_HIGH_YIELD_MODEL=gemini-1.5-flash

# -----------------------------------------------------------------------------
# AUTH CONFIG (optional — frontend)
# -----------------------------------------------------------------------------
# VITE_AUTH_PROVIDERS=email
# VITE_AUTH_EMAIL_ENABLED=true
# VITE_SITE_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# LEGACY — can be removed after full Base44 migration
# These are only used in archived scripts, not in active code
# -----------------------------------------------------------------------------
# VITE_BASE44_APP_ID=
# VITE_BASE44_APP_BASE_URL=
# BASE44_ACCESS_TOKEN=
